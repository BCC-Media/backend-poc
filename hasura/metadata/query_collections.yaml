- name: allowed-queries
  definition:
    queries:
    - name: GET Categories
      query: |-
        query MyQuery {
          categories {
            translated_fields {
              title
            }
          }
        }
